server:
  port: 8080

spring:
  application:
    name: microservices-1
  datasource:
    url: jdbc:postgresql://localhost:5432/example_spring_1
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    database: postgresql
    show-sql: true